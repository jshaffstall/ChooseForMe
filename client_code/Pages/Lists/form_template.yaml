container: {type: ColumnPanel}
components:
- name: label_1
  properties: {text: 'Show the logged in user''s saved lists and allow them to click
      on a list to load it back into the Choose screen.  Add the list name to the
      Choose screen, making it visible only when the list is a saved one.  Allow the
      user to edit the name of the list if it''s a saved list.  If the list has already
      been saved, the save list button on the Choose screen will save it without asking
      for a list name.'}
  type: Label
  layout_properties: {grid_position: 'JTVDCV,VLZHDY'}
is_package: true
